%table{width: "100%"}
  %thead
    %tr
    %th{colspan:"4"} Editing character

  %tbody
    %tr
      %td!= renderTemplate("characters/form", { character: character, errors: errors })

!= link_to("Cancel", character_path(character))
