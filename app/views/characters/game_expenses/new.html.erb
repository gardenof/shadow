<%= stylesheet_link_tag "stylesheet.css" %>
<table width="100%">
  <thead>
    <tr>
      <% if @character.nil? %>
      <% else %>
       <th colspan="4">New Game Expense For <%= @character.name %></th>
      <% end %>
    </tr>
  </thead>

  <tr>
  	<td>
  		<%= render 'form' %>
    </td>
  </tr>

</table>
<div id = "menu">
      <% if @character.nil? %>
      <% else %>
       <%= link_to 'Back', character_path(@character.id) %>
      <% end %>
</div>